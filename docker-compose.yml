version: '3.8'
services:
  warehouse-service:
    build: .
    container_name: warehouse_app
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
    deploy:
      resources:
        limits:
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "2m"
    restart: always
    networks:
      - tst-network

networks:
  tst-network:
    external: true 